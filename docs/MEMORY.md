# Hostylia — Memory.md

**Purpose:** This is the live project ledger. It records what has actually been built, what decisions have been locked, known issues, and the next recommended step. Every AI coding agent must read this before working, and update it after each meaningful task (per `Rules.md` Sec. 36.2). It is the last item in the source-of-truth chain (`Rules.md` Sec. 2) and exists so no one — human or AI — re-derives project state or rebuilds finished work.

**Product:** Hostylia — Smart Residential Management (Hostel & PG platform)
**Company:** Jeevijay Technologies Private Limited
**Build target:** Lovable project `Hostylia: Smart Residential OS`
**Memory version:** 1.0
**Last updated:** July 15, 2026

---

## 1. How to use this file

- **Before coding:** read Sec. 3 (current state) and Sec. 4 (decisions) so you build on what exists and honour locked decisions.
- **After coding:** update Sec. 3 (mark phase progress), append to Sec. 5 (changelog), and record any new Sec. 6 (known issues) or Sec. 7 (pending). Do not delete history — append.
- **Never** contradict a locked decision in Sec. 4 without a PRD/Rules update and an ADR (`Rules.md` Sec. 36.3).

---

## 2. One-paragraph project summary

Hostylia is a multi-tenant hostel/PG management platform for Indian coaching-institute hostels and PGs, built on React + TypeScript + Tailwind + shadcn/ui with a Supabase backend (Postgres, Auth, Storage, Edge Functions), delivered as responsive web only (no native app). It has exactly six roles — Super Admin (Hostylia internal), Hostel Admin (superset per-property owner), Accountant, Warden, Student, Parent — over a Tenant → Organization → Property → Block → Floor → Room → Bed hierarchy. v1 covers property setup, student lifecycle, fees/payments (Razorpay), complaints, gate pass/attendance/mess (Warden-operated), parent portal, and a Super Admin billing console. Security is RLS-first, scoped by `tenant_id` + `property_id`.

---

## 3. Current build state

**Phase model:** 13 phases defined in `Phases.md`. Status is tracked here.

| Phase | Name                                                            | Status        |
| ----- | --------------------------------------------------------------- | ------------- |
| 1     | Project Setup                                                   | ☐ Not started |
| 2     | Authentication                                                  | ☐ Not started |
| 3     | Dashboard                                                       | ☐ Not started |
| 4     | Hostel Management (Property→Bed)                                | ☐ Not started |
| 5     | Student Management                                              | ☐ Not started |
| 6     | Parent Portal                                                   | ☐ Not started |
| 7     | Complaints                                                      | ☐ Not started |
| 8     | Payments                                                        | ☐ Not started |
| 9     | Reports                                                         | ☐ Not started |
| 10    | Notifications                                                   | ☐ Not started |
| 11    | Settings & Ops (Attendance/Gate/Mess/Staff/Super Admin console) | ☐ Not started |
| 12    | Testing                                                         | ☐ Not started |
| 13    | Deployment                                                      | ☐ Not started |

**Status legend:** ☐ Not started · ◐ In progress · ☑ Complete (definition-of-done in `Rules.md` Sec. 6.2 met).

**Status legend correction (2026-07-29):** the table above was never maintained after 2026-07-15 and
understated reality badly. Verified against the codebase and the live database:

| Phase | Name               | Actual status                                                                |
| ----- | ------------------ | ---------------------------------------------------------------------------- |
| 1     | Project Setup      | ☑ Complete — TanStack Start + Tailwind v4 + shadcn, 21 migrations, 51 tables |
| 2     | Authentication     | ◐ Email/password works; **phone OTP is broken** (see Sec. 6)                 |
| 3     | Dashboard          | ◐ Shell/nav complete; **KPI cards are hardcoded stubs**                      |
| 4     | Hostel Management  | ◐ UI complete and now reachable; first blocks/rooms/beds created 2026-07-29  |
| 5     | Student Management | ◐ UI complete; admission, detail, KYC, move-out all render                   |
| 6     | Parent Portal      | ◐ Routes exist, untested                                                     |
| 7     | Complaints         | ◐ Board + SLA badges work; **admin has no actions**; SLA scan not scheduled  |
| 8     | Payments           | ◐ Invoices/aging/revenue work; payments & refunds untested                   |
| 9     | Reports            | ◐ Occupancy/aging/SLA work; **attendance panel is a stub**                   |
| 10    | Notifications      | ◐ In-app works; SMS/WhatsApp/Email disabled; scheduled notices never publish |
| 11    | Settings & Ops     | ◐ Settings + staff work; attendance/gate/mess untested                       |
| 12    | Testing            | ◐ First QA pass 2026-07-29 — see Sec. 5                                      |
| 13    | Deployment         | ☐ Not started                                                                |

The original "No application code written yet" line was false from roughly 2026-07-16 onward.

---

## 4. Locked decisions

These were decided during the context-alignment review and are binding. Changing any requires updating the cited documents and an ADR.

| #   | Decision                                                                                                                                                                                      | Rationale                                                                                                                 | Governs                                                                                  |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| D1  | **Stack = React + Vite + TypeScript (strict) + Tailwind + shadcn/ui + Supabase.** JavaScript-only is reversed; TypeScript is required.                                                        | Lovable-native grain; Zod `z.infer` gives one source of truth for validation + types.                                     | `Rules.md` Sec. 3–4, `Architecture.md` Sec. 2.1                                          |
| D2  | **Tenancy root = Tenant → Organization → Property.** Both levels kept.                                                                                                                        | Tenant = Hostylia billing account (subscription state); Organization = legal GST entity. Different lifecycles/RLS owners. | `PRD.md` Sec. 5, `DB-Schema.md`, `Architecture.md` Sec. 10.1                             |
| D3  | **E-signature = click-wrap in v1** (accepted_at, IP, user-agent, hash in `agreements.signature_evidence`, `signature_method=CLICK_CONSENT`). Aadhaar eSign/DSC is Stage 2 (`EXTERNAL_ESIGN`). | Keeps admission fully digital with zero vendor dependency; schema already supports both.                                  | `PRD.md` Sec. 6.2.4, `Architecture.md` Sec. 2.3                                          |
| D4  | **MEMORY.md created; Test-Checklist.md dropped** (per-phase checklists live in `Phases.md`).                                                                                                  | One progress ledger; no seventh file to keep in sync.                                                                     | `Rules.md` Sec. 2, all doc precedence lists                                              |
| D5  | **Palette from the logo.** Primary deep teal `#00696F`; success forest green `#15803D`; navy ink `#0A141E`; brand cyan `#00D8CC` focus/dark accent. Brand teal is never a status colour.      | Brand teal fails AA as a button fill; teal/green would collide in the occupancy grid. All tokens AA-verified.             | `Design.md` Sec. 2, Sec. 4                                                               |
| D6  | **Build target stays Lovable + Supabase. Redis deferred, seams documented.**                                                                                                                  | Redis is out-of-band and Lovable can't manage it → reintroduces hallucination risk. Not needed at target scale.           | `Architecture.md` Sec. 26.3, `Rules.md` Sec. 3.4                                         |
| D7  | **Redis's only intended job = OTP/API rate limiting** (not caching/queue — those are already Postgres-backed via `background_jobs`/`idempotency_keys`).                                       | Names the one defensible future use so it doesn't sprawl.                                                                 | `Architecture.md` Sec. 26.3                                                              |
| D8  | **v1 rate limiting = Postgres limiter behind `checkRateLimit(key, limit, window)`.** Upstash Redis is the documented Stage-3 swap (function body only; no call-site change).                  | Handles OTP/SMS abuse at target scale without a second datastore.                                                         | `Rules.md` Sec. 29.5, `DB-Schema.md` Sec. 75a `rate_limits`, `Architecture.md` Sec. 26.3 |

### Additional standing rules from the review

- **Edge Functions stay short + idempotent**; bulk/recurring work goes to `background_jobs` in bounded batches (`Rules.md` Sec. 23.1a).
- **All DB access via the Supabase pooler** (Supavisor/PgBouncer) — the real scaling limit is connections, not rows (`Rules.md` Sec. 23.1b).
- **One Zod schema per entity**, imported by form + Edge Function + CSV importer + type; client and server validate the same object (`Rules.md` Sec. 29.4).
- **RLS is the primary security boundary**, not the RBAC matrix; the PRD permission table is a UI/policy layer, enforcement is server-side RLS scoped by `tenant_id`+`property_id` (`Rules.md` Sec. 15, Sec. 8.7).
- **Inbound webhooks (Razorpay callbacks) are v1**; outbound public API/webhooks are Stage 3 (`PRD.md` Sec. 12, `DB-Schema.md` Sec. 74).

---

## 5. Changelog

| Date       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | By               |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| 2026-08-21 | **Added: Student module-level permissions (Read/Edit/No-Access), admin-configurable per student.** Extended the exact same granular-permission architecture Warden/Accountant already have (`role_assignments.permissions` JSONB + `can_*`/`has_*_permission` SQL functions) to STUDENT rows instead of building a parallel system — students already get their own `role_assignments` row (role=STUDENT, one per student, created for tenant/property scoping) which had never been used for permissions before this. Every prior student-self RLS policy was hardcoded unconditionally true (`is_owning_student()`/bare `profile_id = auth.uid()`, no permission check) — confirmed live via `pg_policies` before writing anything. Migration `20260821060645_student_module_permissions.sql` (+ follow-up fix `20260821061003_...`): new `has_student_permission_for_user()` helper (generic key+default, mirrors `has_role_permission`) and 13 per-module `can_student_view_*`/`can_student_edit_*` functions covering the 7 real student-facing modules (Profile, Attendance, Notices, Complaints, Finance, Gate Pass, Mess — not Allocations/Visitors/Support, which have no dedicated student page); rewired ~20 existing RLS policies across `students`/`agreements`/`allocations`/`documents`/`attendance`/`complaints`/`complaint_comments`/`notices`/`invoices`/`payments`/`payment_orders`/`refunds`/`deposit_ledger_entries`/`gate_events`/`gate_passes`/`gate_pass_approvals`/`mess_feedback`/`mess_menus` to route through them, same joins, only the terminal boolean swapped. 2 boolean keys per module (`student_<module>_view/_edit`, prefixed to avoid colliding with WARDEN/ACCOUNTANT's bare key names in the same JSONB column's CHECK constraint allow-list); absent key defaults to Edit (both true) so an unconfigured student sees zero behavior change — verified live pre/post. Caught and fixed a real regression before shipping: the first draft's Profile functions used `is_own_student()` (excludes soft-deleted students via `deleted_at IS NULL`) where the original policies had no such filter — a soft-deleted APPLICANT-status test row lost self-read access it previously had; fixed by switching to `is_owning_student()` (no deleted_at check) to exactly preserve prior behavior, confirmed via live before/after query against the real affected row. Frontend: `useStudentPermissions()` (`src/lib/staff-scope.ts`, mirrors `useWardenPermissions`), `StudentModuleGuard` (`src/components/dashboard/RoleGuard.tsx`, same redirect-to-/403 pattern as `RoleGuard`) wraps all 6 gated student routes; `BOTTOM_NAV.STUDENT` entries gained a `module` key so `student.tsx` filters nav items on No-Access; write actions (new complaint, post comment, rate, pay invoice, request/cancel/reissue gate pass QR, submit mess feedback, edit profile, upload KYC) hidden behind new optional `canWrite`/`canPay`/`canEdit` props threaded into `ComplaintCommentThread`/`RatingWidget`/`StudentFeesList` (default `true`, so Warden/Admin-facing callers of these same components are unaffected) plus inline route-level conditionals; `student.home.tsx`'s 4 dashboard sections (Attendance/Fees/Gate Pass/Mess) hide per-module too. Admin UI: new `StudentPermissionCard` (`src/components/students/StudentPermissionCard.tsx`, 3-way `ToggleGroup` per module — not the 4-verb checkbox grid Warden/Accountant use, since students only need 3 tiers) added to the existing `admin.students.$id.index.tsx` detail page; new `getStudentPermissions`/`updateStudentPermissions` server fns (`src/lib/admin-staff.functions.ts`) follow `updateStaff`'s exact shape (admin-gated PATCH of `role_assignments.permissions`, no new privileged flow) — only ever updates an existing STUDENT role_assignments row, never inserts (an unlinked student with no portal account has nothing to gate). Verified live end-to-end (`SET LOCAL ROLE authenticated` + real `request.jwt.claims`, not just function-level checks): No-Access on Finance → 0 invoice rows visible; Read-tier on Complaints → SELECT works, INSERT correctly rejected by RLS (`42501`); reset back to unconfigured → original unconditional behavior confirmed returned. `tsc --noEmit` back to the 15-error pre-existing baseline (none in touched files), `bun run build` passed, `get_advisors` shows only the same "SECURITY DEFINER RPC-callable" informational warning every existing `can_*`/`has_*_permission` function already carries — no new finding class.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Claude           |
| 2026-08-14 | **Investigated: Super Admin "Mark Resolved" showing "SUPER_ADMIN required".** Exhaustively audited the whole chain (`assertSuper` → `is_super_admin()` RPC → `support_tickets_update_super` RLS policy → `requireSupabaseAuth`/`attachSupabaseAuth` middleware) against the exact real reported ticket ("staff section", tenant `sams`) and both real registered Super Admin accounts, live, via `BEGIN…ROLLBACK` simulation of the exact SQL `updateSupportTicket` performs — **the backend/RLS/RPC layer is 100% correct**: both accounts pass `is_super_admin()`, the RLS-gated UPDATE succeeds and sets status/resolved_by/resolved_at/resolution_note exactly as expected, and — as a security regression check — the same UPDATE attempted as a real Hostel Admin correctly affects 0 rows. This is architecturally identical to `assignTenantSubscription`/`setTenantCustomPrice`/`upsertFeatureOverride` (all confirmed working), so there is no code asymmetry between "Mark Resolved" and any other working Super Admin mutation. Conclusion: the reported failure is a **client-side stale-identity mismatch**, not an authorization bug — Supabase persists its session to localStorage shared across same-origin tabs, so a different account signing in (this tab or another sharing that storage) can leave React Query's in-memory cache still showing data fetched under the _previous_ identity even though every subsequent server-function call correctly re-authenticates fresh as the new one — explaining exactly the reported symptom (ticket detail visibly loaded, but the mutation correctly rejected for whatever identity was _actually_ current at click-time). Fix (`src/routes/__root.tsx`): a `supabase.auth.onAuthStateChange` listener now clears the entire React Query cache whenever the authenticated user id changes or on sign-out — closes this class of bug without touching any authorization check, RLS policy, or `assertSuper`/`is_super_admin` logic, and is not a client-side _role_ check (it never decides access — it only stops stale data from lingering across an identity change; the server still independently re-validates every request exactly as before). No support-ticket backend code was changed. Verified live (`BEGIN…ROLLBACK`, real accounts, real ticket, zero permanent mutations): Super Admin (Queen) → status/resolved_by/resolved_at/resolution_note all set correctly; Hostel Admin of the ticket's own tenant → 0 rows updated (correctly denied) — confirms both the fix's target class and that security is unchanged. `tsc --noEmit` unchanged 14-error baseline, `eslint --fix` clean, `bun run build` passed. **Remaining/user action**: this fix prevents the mismatch going forward but cannot retroactively repair an already-stale session sitting open in a browser tab right now — if "SUPER_ADMIN required" recurs immediately after this fix ships, a hard refresh or fresh sign-in of that specific tab will confirm which account is actually active, since the underlying authorization logic is already proven correct against real data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Claude           |
| 2026-08-14 | **Completed: Admin ↔ Super Admin Support Ticket resolution/notification flow — audit found most of it already worked, only "Resolved by" + Admin notification were genuinely missing.** Audit confirmed create/visible-to-Super-Admin/reply/status-change/see-status already worked correctly (built in the prior session's support-ticket task) — none of that was rebuilt. Also found a **full, real-time in-app notification system already exists and is already used for the exact same kind of event**: `notifications` table + `useMyNotifications()`/`NotificationBell` (Topbar, realtime via `postgres_changes`, unread badge, mark-read) + the precedent trigger `fn_notify_admins_complaint_resolved` (20260730121453) that notifies Hostel Admin on a complaint's RESOLVED transition. Reused this exact pattern rather than building anything new. Migration `support_ticket_resolution_and_notify`: (1) added the genuinely-missing `support_tickets.resolved_by` column (resolved_at/resolution_note already existed); (2) new trigger `trg_support_tickets_notify_resolved` / `fn_notify_admin_ticket_resolved()` (SECURITY DEFINER, `REVOKE ... FROM PUBLIC,anon,authenticated`, same shape as the complaint-resolved trigger) that fires `AFTER UPDATE ... WHEN (NEW.status='RESOLVED' AND OLD.status IS DISTINCT FROM NEW.status)` and inserts one `notifications` row (IN_APP, DELIVERED immediately, payload `{title, body: subject, ticket_id}`) for the ticket's `created_by` — a trigger rather than an app-layer call because resolution can happen via a plain `.update()` from any code path and `notifications` deliberately has no client INSERT policy, exactly the same reasoning the complaint-resolved trigger's own comment already states. App changes: `updateSupportTicket` now sets `resolved_by` alongside the existing `resolved_at` logic when status→RESOLVED; `getSupportTicketDetail`/`listSupportTickets` resolve the `resolved_by` profile name via the same service-role lookup already used for `created_by`/`assigned_to`. Super Admin's ticket dialog (`super-admin.support-tickets.tsx`) now separates "Mark Resolved" (dedicated button, requires a non-empty resolution note, only path to RESOLVED) from the generic status dropdown (which no longer offers RESOLVED directly) — fixes a real gap in the prior session's UI where selecting RESOLVED from the dropdown could fire with an empty note; shows a read-only Resolved-by/Resolved-at/Resolution block once resolved. Hostel Admin's ticket detail (`admin.support.$id.tsx`) now shows the same Resolved-by ("Hostylia Support" generic label, consistent with the page's existing message-author convention of never resolving individual Super Admin staff names to the Admin) / Resolved-at / Resolution block, and marks the ticket's resolution notification read (via the pre-existing `markNotificationRead`) when the Admin opens that ticket. `NotificationBell.tsx` gained click-to-navigate: a notification whose payload carries `ticket_id` is now a real button that marks itself read and routes to `/admin/support/$id` — the _only_ change to the shared Bell component, safe for every other role since only this one event type ever carries `ticket_id`. Deliberately **not built**, per explicit instruction not to invent a second mechanism: a separate per-nav-item unread badge on the "Hostylia Support" sidebar entry — the existing global Bell badge (already covers this notification automatically once the trigger fires) was judged sufficient; noted as an optional enhancement, not a gap. Also deliberately not built: a reopen workflow (RESOLVED/CLOSED → OPEN) — no existing PRD/schema requirement for it, Admin can already reply while status is RESOLVED (pre-existing, unchanged) but there is no explicit reopen action; reported as an optional future enhancement, not implemented. Verified live end-to-end (all `BEGIN…ROLLBACK` except where noted, against real tenant/user IDs): ticket creation, Super Admin visibility, resolution with `resolved_by`/`resolved_at`/`resolution_note` all persisting correctly, the trigger firing and inserting exactly the expected `notifications` row (event_type `SUPPORT_TICKET_RESOLVED`, channel `IN_APP`, status `DELIVERED`, `read_at` null, payload containing the ticket's real subject and id), tenant isolation across two different real tenants (Admin of Tenant A sees only Tenant A's ticket). Also discovered, without touching it, a real ticket ("staff section", OPEN) already created by a real Hostel Admin through the live UI — confirms ticket creation already works in production. Confirmed via `git status`/`git diff --stat` that zero complaint-related files or the existing `fn_notify_admins_complaint_resolved` trigger were touched (TEST 7 regression). `tsc --noEmit` back to the 14-error pre-existing baseline; `eslint --fix` clean; `bun run build` passed; `get_advisors` shows zero new findings (the new trigger function is revoked from all roles, same as its complaint-resolved precedent, so it doesn't even appear in the SECURITY DEFINER advisory list).                                                                                                                                                                                                                                                                                                                                                      | Claude           |
| 2026-08-13 | **Added: Hostylia Support Ticket flow (Hostel Admin ↔ Super Admin) — audit confirmed this was genuinely missing, not a duplicate of `complaints`.** Investigated first: no `support_ticket`/`ticket`/"contact support" system existed anywhere in the DB schema or codebase (only marketing-site "contact"/"support" copy, and the unrelated `support_sessions` impersonation table) — confirmed via full grep of `src/` and `docs/DB-SCHEMA.md`. Built as a deliberately separate concept from `complaints` (Student→Warden→Admin operational lifecycle — completely untouched, zero shared tables/routes/RLS). Migration `add_support_tickets`: new `support_tickets` (tenant_id, created_by, subject, category [9 values matching the requested taxonomy], priority [LOW/MEDIUM/HIGH/URGENT], status [OPEN/IN_PROGRESS/WAITING_FOR_ADMIN/RESOLVED/CLOSED], description, assigned_to, `support_session_id` FK to the existing `support_sessions` table for the impersonation link, resolution_note, resolved_at, closed_at) and `support_ticket_messages` (ticket_id, tenant_id, author_id, `is_internal_note`, message — append-only, same immutability pattern as `audit_logs`). RLS: SELECT scoped like `subscriptions_select_admin` (Super Admin sees all, a tenant's own Hostel Admin sees only their tenant); Hostel Admin can INSERT a ticket for their own tenant only; every other ticket-field mutation (status/priority/assignment/resolution) is Super-Admin-only via RLS UPDATE — the one Hostel-Admin-permitted transition (RESOLVED→CLOSED) goes through new `fn_close_own_support_ticket()` (SECURITY DEFINER, same pattern as `fn_cancel_own_subscription`) instead of a direct RLS grant; messages: Hostel Admin can read/write non-internal messages for their tenant only, internal notes are Super-Admin-only both to read and write (RLS-enforced, not just UI-hidden). **Attachments reuse the existing generic `documents` registry** (extended `documents_owner_type_check` to add `'SUPPORT_TICKET_MESSAGE'`, no new attachment table) plus one narrow new `documents` SELECT policy scoped strictly to that owner_type for Super Admin (the pre-existing "documents admin all" policy already covered Hostel Admin's own tenant) — modeled on the already-existing precedent of `receipts_finance_select` including `is_super_admin()` directly in a storage-adjacent policy. New private storage bucket `support-ticket-attachments` with symmetric read/write RLS (`is_super_admin() OR has_tenant_role(HOSTEL_ADMIN)` on the tenant-id path segment), same shape as the existing `complaint-media`/`receipts` bucket policies. New server functions: `src/lib/support-ticket.functions.ts` — Hostel Admin's `createSupportTicket`/`closeSupportTicket`/`addSupportTicketMessage` (RLS-protected direct writes, no service-role); Super Admin's `listSupportTickets`/`getSupportTicketDetail` (service-role profile-name resolution via the same pattern `getTenantHostelAdmins` established, since `profiles` RLS is self-access-only with no Super-Admin-inclusive policy)/`updateSupportTicket` (status/priority/assignment/resolution, audit-logged via the existing service-role `audit_logs` insert pattern)/`linkSupportSessionToTicket` (connects an already-started, completely unmodified `startSupportSession` session to the ticket that prompted it). UI: new Hostel Admin sidebar item **"Hostylia Support"** (`admin.support.tsx` list+create, `admin.support.$id.tsx` detail/reply/attach/close) placed directly after the existing, untouched "Complaints" item — two visibly separate sections, never merged; new Super Admin sidebar item **"Support Tickets"** (`super-admin.support-tickets.tsx`, list with status filter + a detail dialog covering reply/internal-note/status/priority/"Start impersonation for this ticket"). Verified live end-to-end (all `BEGIN…ROLLBACK`, zero permanent mutations, against real tenant/user IDs): ticket creation; cross-tenant isolation (a different tenant's real Hostel Admin sees 0 rows); Super Admin cross-tenant visibility (1 row); internal-note invisibility (Admin sees 1 of 2 messages, 0 internal notes); the close-guard correctly rejecting a still-OPEN ticket and correctly succeeding RESOLVED→CLOSED; the resulting `audit_logs` row confirmed written; attachment upload via `documents` + Super Admin cross-tenant read of that attachment metadata, both confirmed. `tsc --noEmit` back to the 14-error pre-existing baseline (two `Record<string,unknown>` typing errors introduced by the initial draft of `updateSupportTicket` were fixed with an explicit patch-object type, not a workaround cast); `eslint --fix` clean (same 5 pre-existing `no-explicit-any`, plus `assertSuper` exported from `super-admin.functions.ts` — one keyword added, zero new `any` — confirmed via `git diff`); `bun run build` passed, both new routes registered in `routeTree.gen.ts`; `get_advisors` shows exactly the expected new `fn_close_own_support_ticket` SECURITY DEFINER advisory pair, no RLS-missing-policy warnings, no unexpected findings.                                                                                                                                                                                                                                                                                                                                                                                                        | Claude           |
| 2026-08-13 | **Added: Super Admin subscription expiry/renewal visibility (Billing table + Dashboard alerts) — audit found genuine gaps, no DB/RLS changes needed.** Audit confirmed A/B/C/H/I (ACTIVE/TRIAL/PAST_DUE/CANCELLED/tenant-name) were already visible via the existing status badge + tenant join in `listSubscriptionsWithPlan`/Billing table and the Dashboard's existing "Subscription health" status-count card + "Past-due subscriptions" KPI — none of that was touched. Genuinely missing: D (derived "already past period end" signal), E/F ("ending soon" for both period end and `trial_ends_at`), G (`cancel_at_period_end` visibility) — `trial_ends_at` and `cancel_at_period_end` are pre-existing `subscriptions` columns that were simply never selected or rendered anywhere. Fix is query+UI only, **zero migrations, zero RLS changes**: extended `listSubscriptionsWithPlan`'s existing select to also fetch `trial_ends_at,cancel_at_period_end` (same already-authorized `subscriptions_select_admin` RLS read, no new server fn). Added `src/lib/subscription-expiry.ts` — a small, shared, DB-write-free helper (`daysRemaining`, `relevantEndDate` [trial_ends_at for TRIAL, else current_period_end], `getExpiryIndicator` → `ENDING_SOON` (≤7d) / `ENDS_TODAY` (0d) / `ENDED` (<0d, never for CANCELLED) / `null`, `formatRemaining`) reused by both Billing and Dashboard so the threshold/logic lives in exactly one place. **Explicitly did not invent an `EXPIRED` DB status** — "Ended" is purely a derived UI badge computed at render time against the existing status; a subscription whose period has passed simply keeps showing its real stored status (e.g. still `TRIAL`) plus the derived badge, consistent with the already-confirmed absence of any auto-expiry cron. Billing table gained a "Remaining" column (day count + colored Ending-soon/Ends-today/Ended badge, semantic `warning`/`destructive` tokens via existing `toneClasses`, never raw hex) and a "Cancel at period end" outline badge next to the status when `cancel_at_period_end=true` and status isn't already CANCELLED; `ManageSubscriptionDialog` gained a "Trial ends" field (TRIAL only) and an amber-toned "Scheduled to cancel at period end" notice. Dashboard gained a new "Subscription alerts" card (only rendered when at least one alert exists) with three clickable rows → `/super-admin/billing`: overdue (reuses the existing `pastDueCount`, not recomputed), ending soon, trials ending soon — explicitly not a new notification system, just three more counts derived from data the page already fetches. **Regression note (unrelated file, root-caused and fixed):** extending `listSubscriptionsWithPlan`'s select string widened Supabase's generated-type inference failure (the same pre-existing "GenericStringError" class already present in 5 other places in this codebase) so broadly that it broke `super-admin.dashboard.tsx` too, a file this task never touched — fixed at the root by giving `listSubscriptionsWithPlan` an explicit `SubscriptionWithPlan` return type (exported, now reused by both Billing and Dashboard instead of each redefining/casting), restoring `tsc` to the exact 14-error pre-existing baseline. Verified against real live data (today = 2026-08-13): both real TRIAL subscriptions with `trial_ends_at` in the past correctly render "Ended" (a live, organic proof of the earlier-flagged no-auto-expiry gap — they're still sitting at `status='TRIAL'`indefinitely); real ACTIVE Starter with a real ₹120 custom price still shows correct effective price and is correctly excluded from nothing (TEST H); TRIAL Durga hostel's real ₹120 custom price does **not** inflate `active_subscriptions` (still 2) or `mrr_paise` (still ₹260 = only the 2 real ACTIVE rows) — TEST I confirmed live. Simulated (`BEGIN…ROLLBACK`, zero permanent mutations) "ends today" (`current_period_end = now()` → 0 days, ENDS_TODAY) and `cancel_at_period_end=true` while status stays ACTIVE (TEST B/G) — both correct, both fully rolled back. RLS isolation re-confirmed unchanged: a real Hostel Admin sees exactly 1 subscription row (their own tenant), 0 from any other tenant (TEST J) — same pre-existing `subscriptions_select_admin` policy, untouched. `tsc --noEmit` back to the 14-error baseline, `eslint --fix` clean (5 pre-existing `no-explicit-any`, confirmed via `git diff` none new), `bun run build` passed. No migration file this task — first task in this changelog with zero DB changes, by design.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Claude           |
| 2026-08-13 | **Added: Super Admin Billing → 30-day churn details, plus the Hostel Admin self-cancellation flow that had to exist first for churn to ever be populated.** Investigated first: `fn_get_platform_metrics()`'s churn definition (`status='CANCELLED' AND cancelled_at >= now()-30d`, divided by subscriptions created ≥30 days ago) was already correct and is **unchanged** — only added a `churned_count_30d` field alongside the existing `churn_30d` percentage (both from the same already-computed `v_cancelled_30`, no new query). The bigger finding: there was **no cancellation flow anywhere in the codebase** — `subscriptions_write_super` RLS only permits Super Admin writes (no Hostel-Admin self-service bypass), `admin.settings.tsx` had no Billing tab, and no `subscription_cancellations`-shaped table existed (confirmed via full-codebase and DB-schema grep; `docs/DB-SCHEMA.md`'s `v_saas_metrics` mention was a "potential fields" sketch, never built). Built the minimum: migration `add_subscription_cancellation_feedback` adds `subscription_cancellations` (subscription_id, tenant_id, cancellation_reason [CHECK'd against the 8 listed reasons], cancellation_reason_other, continue_in_future, additional_feedback, cancelled_by, cancelled_at, created_at) with RLS SELECT scoped exactly like `subscriptions_select_admin` (`is_super_admin() OR has_tenant_role(HOSTEL_ADMIN)` for that tenant) and **no INSERT/UPDATE/DELETE policy for `authenticated` at all** — same "no direct client write" pattern `audit_logs` already uses. New `fn_cancel_own_subscription(tenant_id, reason, reason_other, continue_in_future, feedback)` (SECURITY DEFINER, same pattern as `fn_assign_hostel_admin`) does its own internal `has_tenant_role(HOSTEL_ADMIN)` check, only finds/cancels the tenant's ACTIVE subscription (a TRIAL cannot be "cancelled" this way — verified live, correctly rejected with "No active subscription found"), writes the feedback row and an `audit_logs` row (`SUBSCRIPTION_CANCELLED_BY_HOSTEL_ADMIN`) in the same transaction. New server fns: `cancelMySubscription` (`src/lib/hostel.functions.ts`, thin RPC wrapper) and `listChurnedTenants` (`src/lib/super-admin.functions.ts`, joins `subscriptions`+`tenants`+`plans`+left-joined `subscription_cancellations` within the same 30-day window, with optional plan/reason/date-range filters — deliberately the _same_ churn definition, not a second one). Schema: new `src/schemas/subscription.ts` (`cancellationReasonSchema`, shared by the client form and server validator, per the one-schema-per-entity convention). UI: `admin.settings.tsx` gained a "Billing" tab (current plan/status/effective price/dates + "Cancel subscription" → full feedback dialog: reason select incl. "Other" free-text, Yes/No continue-in-future radio, optional feedback textarea, Cancel/Confirm Cancellation — never cancels until confirmed). `super-admin.billing.tsx`'s churn KPI card now also shows "N tenants churned" + a "View details" link opening the new `ChurnDetailsDialog` (plan/reason/date-range filters, table with tenant/plan/started/cancelled/usage-duration/effective-price/reason/continue/feedback; usage duration computed from real `starts_at`→`cancelled_at` via date-fns, never `today()`; missing feedback on historical cancellations — none exist yet, but the left-join guarantees it — renders "Not provided", never fake data). Verified live end-to-end (all `BEGIN…ROLLBACK`, zero permanent mutations): real Hostel Admin cancels their tenant's real ACTIVE Starter subscription with feedback → subscription flips to CANCELLED, feedback row persists exactly as submitted, audit_logs row written; usage-duration math confirmed exact (17 days 2h → correctly rendered "17 days"); a cancellation backdated 40 days is correctly excluded from both `churned_count_30d` and the churn-details query (edge case D), while the real recent one is correctly included (edge case E); this same real scenario also demonstrated _why_ the count matters — with a very young dataset, `churn_30d`'s percentage stayed 0.0% (denominator of subscriptions-older-than-30-days was 0) despite one real churned tenant, exactly the UX gap this task's `churned_count_30d` addition fixes; a different tenant's real Hostel Admin account could not see this tenant's cancellation feedback (0 rows, RLS tenant isolation); a real ACCOUNTANT/non-Super-Admin account could not call `fn_cancel_own_subscription` for someone else's tenant. No changes to MRR calc, ACTIVE-subscription count, TRIAL exclusion, subscription status model, or any other RLS policy. `tsc --noEmit` back to the 14-error pre-existing baseline after two small `as unknown as` casts for a Supabase-generated-types gap on the reverse-FK embed (same "GenericStringError" class already present elsewhere in this codebase for complex embedded selects — not a new problem class, just one more instance of an existing one); `eslint --fix` clean (8 pre-existing `no-explicit-any` errors across `super-admin.functions.ts`/`admin.settings.tsx`, confirmed via `git diff` none newly introduced); `bun run build` passed; `get_advisors` shows exactly one new expected finding pair for `fn_cancel_own_subscription` (anon/authenticated EXECUTE grant warnings, identical in kind to every other SECURITY DEFINER function already in the codebase) — no unexpected findings, no RLS-missing-policy warning for the new table. | Claude           |
| 2026-08-13 | **Added: dynamic Trial Period configuration for plans (Super Admin → new Plans page), default 7 days.** Investigated first: `plans.trial_days` already existed (NOT NULL, `plans_trial_days_check CHECK (trial_days >= 0)` already enforced at the DB layer) and was **already used dynamically** in both places a trial starts — `fn_provision_tenant()` (self-serve signup: `trial_ends_at = now() + (p.trial_days                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |                  | ' days')::interval`) and `assignTenantSubscription()`(Super Admin manual assignment: same formula in JS) — so the trial *calculation* logic required zero changes; nothing was hardcoding 7 anywhere. The two actual gaps: (1) the column's DEFAULT was 0, not 7, so a newly-created plan with no explicit trial_days silently got a 0-day trial; (2) there was no Super Admin UI anywhere to configure a plan's trial_days —`listPlans`was read-only, used only to populate the existing "Assign subscription" dropdown. Fix: migration`set_plans_trial_days_default_7`—`ALTER TABLE plans ALTER COLUMN trial_days SET DEFAULT 7`, touching only the column default, not any existing row (live Starter=14, Professional=14, Enterprise=0 all confirmed unchanged after). Added `updatePlanTrialDays` server fn (`src/lib/super-admin.functions.ts`) — Super-Admin-only, Zod-validated non-negative integer, writes only `trial_days`(no price/billing-interval/other plan field touched), audit-logged via the existing service-role`audit_logs` pattern (`PLAN_TRIAL_DAYS_CHANGED_BY_SUPER_ADMIN`). Gated by the pre-existing `plans_write_super`RLS policy (is_super_admin only, same one`assignTenantSubscription`'s reads and `setTenantCustomPrice`already rely on) — zero RLS changes. New page`super-admin.plans.tsx`(added to`SIDEBAR_NAV`) lists all plans with price/trial period/status and an "Edit trial period" action per row. Also changed `assignTenantSubscription`'s defensive `plan.trial_days ?? 0`to`?? 7`for consistency with the new default (dead-code path today since the column is NOT NULL, not a behavior change for any real plan). Verified live (all`BEGIN…ROLLBACK`, zero permanent mutations): a plan inserted with no explicit trial_days got exactly 7; forcing `fn_provision_tenant`to resolve to a temporarily-Starter-only-active catalog with`trial_days=7`produced`trial_ends_at - starts_at = exactly 7 days`; same test with Professional at `trial_days=30`produced exactly 30 days; a real non-Super-Admin (ACCOUNTANT) account's direct UPDATE against`plans.trial_days`affected 0 rows under RLS. **Missing/PRD gap explicitly NOT built, per instruction not to invent unrelated behavior**: there is no automated trial-expiry job anywhere in the codebase (no`background_jobs`entry, cron, or Edge Function references`trial_ends_at`) — a TRIAL subscription whose `trial_ends_at`has passed does not automatically transition to PAST_DUE/CANCELLED/ACTIVE; it just silently remains`status = 'TRIAL'`until a human (Super Admin) changes it. This is a real, separate missing feature, flagged rather than silently built.`tsc --noEmit`unchanged 14-error baseline,`eslint --fix`clean (same 5 pre-existing`no-explicit-any`in`super-admin.functions.ts`, confirmed via `git diff`none new),`bun run build`passed (also regenerated`routeTree.gen.ts`for the new route),`get_advisors` shows zero new findings (no new SECURITY DEFINER function this time). | Claude |
| 2026-08-13 | **Added: Super Admin Billing → per-tenant custom subscription pricing ("Manage" dialog).** New feature, not a bug fix: Super Admin can now override a single tenant's effective subscription price without touching the shared `plans.price_paise` row other tenants on the same plan still use. Investigated first: `subscriptions` had no pricing-override column, and the existing `subscriptions_write_super` RLS policy (`is_super_admin(auth.uid())`, all commands, no HOSTEL_ADMIN bypass — same one `assignTenantSubscription` already relies on) already covers a new nullable column with zero RLS changes needed, so this reused the established direct-client-write pattern rather than adding a new SECURITY DEFINER RPC. Migration `add_subscription_custom_price`: adds `subscriptions.custom_price_paise bigint NULL` (NULL = standard plan price, fully backward-compatible — every existing row defaults to NULL) with a `CHECK (custom_price_paise IS NULL OR custom_price_paise >= 0)` constraint, and updates `fn_get_platform_metrics()`'s MRR sum to use `COALESCE(s.custom_price_paise, p.price_paise)` per ACTIVE subscription (same MONTHLY/QUARTERLY÷3/YEARLY÷12/WEEKLY×4 normalization as before, untouched) — TRIAL rows remain fully excluded from MRR and `active_subscriptions` regardless of whether they carry a custom price, exactly preserving the two prior MRR/active-count fixes. Added `setTenantCustomPrice` server fn (`src/lib/super-admin.functions.ts`) — Super-Admin-only, validates the price is a non-negative integer or `null`, writes only `custom_price_paise` (never plan/status/period fields, so it can never turn a TRIAL into a paid subscription), and audit-logs old/new price via the existing service-role `audit_logs` insert pattern (`SUBSCRIPTION_CUSTOM_PRICE_CHANGED_BY_SUPER_ADMIN`). Extended `listSubscriptionsWithPlan`'s select to include `custom_price_paise`, `current_period_start`, and a `tenants(display_name)` join (tenants' SELECT RLS already includes `is_super_admin()`, so no RLS change needed there either). UI (`super-admin.billing.tsx`): added a "Manage" button per subscription row opening a new `ManageSubscriptionDialog` showing tenant/plan/status/interval/standard price/period start+end plus a "Use custom price for this tenant" toggle + rupee input; the table's Price column now shows the effective price (custom override if set, else standard) with a small "Custom" badge when overridden, and the tenant name (previously only a truncated tenant_id). Verified live (all wrapped in `BEGIN…ROLLBACK`, zero permanent mutations): setting Tenant A's ACTIVE Professional subscription to a ₹500 custom price left `plans.price_paise` at ₹140 unchanged and correctly shifted MRR from ₹255 to ₹615 (₹500 + the other tenant's standard ₹115 Starter); giving a TRIAL subscription a ₹2500 custom price left MRR and `active_subscriptions` completely unchanged (still ₹255 / 2) since TRIAL rows are excluded regardless of price; the negative-price CHECK constraint correctly rejected `-100`; a real non-Super-Admin (ACCOUNTANT) account's direct UPDATE attempt against `custom_price_paise` affected 0 rows under RLS, while the same UPDATE from a Super-Admin session succeeded — confirming only Super Admin can change tenant pricing, exactly as required. `get_advisors` shows zero new findings (no new SECURITY DEFINER function was introduced this time, unlike the Hostel Admin assignment feature). `tsc --noEmit` unchanged 14-error pre-existing baseline, `eslint --fix` clean (the 5 remaining `@typescript-eslint/no-explicit-any` errors in `super-admin.functions.ts` are all pre-existing, confirmed via `git diff` — zero new `any` introduced), `bun run build` passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Claude           |
| 2026-08-13 | **Fixed: Super Admin Billing MRR incorrectly included TRIAL subscriptions' plan price.** Same root-cause family as the earlier `active_subscriptions` KPI bug (2026-08-13, below): `fn_get_platform_metrics()`'s MRR `SUM(...)` query filtered `WHERE s.status IN ('ACTIVE','TRIAL')`, so a TRIAL subscription with a non-zero configured plan price (e.g. TRIAL Starter ₹115/month) was counted as recurring revenue even though no payment was actually recurring. Verified live: with ACTIVE Starter ₹115 + ACTIVE Professional ₹140 + TRIAL Starter ₹115 + 2× TRIAL Enterprise ₹0, MRR showed ₹370 instead of the correct ₹255. Fix (migration `fix_mrr_excludes_trial_subscriptions`): changed only the MRR query's `WHERE` clause to `status = 'ACTIVE'` — the billing-interval normalization CASE (MONTHLY/QUARTERLY÷3/YEARLY÷12/WEEKLY×4) is untouched, `active_subscriptions`/`churn_30d`/`tenants_by_status` logic untouched. No app code changed — `super-admin.billing.tsx` and `super-admin.dashboard.tsx` both already just read `metrics.mrr_paise` from this one RPC, and the subscriptions table still renders each row's raw configured plan price (TRIAL Starter still correctly shows ₹115/month — display logic is intentionally separate from the aggregate). Verified live post-fix (`BEGIN…ROLLBACK`, no permanent mutation): `mrr_paise: 25500` (₹255), `active_subscriptions: 2` (unchanged), `churn_30d: 0` (unchanged). Also unit-tested the normalization CASE in isolation against synthetic ACTIVE rows for all four intervals (MONTHLY/QUARTERLY/YEARLY/WEEKLY, all correct) and two TRIAL rows (non-zero and zero price, both correctly contribute 0 to MRR). `get_advisors` shows the identical pre-existing advisory for `fn_get_platform_metrics` (no new findings). Zero app files touched this task (confirmed via `git status`), so `tsc`/`eslint`/`bun run build` are all identical to the untouched-baseline results. Also wrote the local migration file for this fix (and manually reconstructed the file for the 2026-08-13 `active_subscriptions` fix and the earlier `add_fn_assign_hostel_admin` migration) since `apply_migration` calls this session were not syncing back into `supabase/migrations/` locally — worth investigating as a tooling/environment gap if it recurs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Claude           |
| 2026-08-13 | **Fixed: Super Admin Billing/Dashboard "Active subscriptions" KPI counted TRIAL as active.** `fn_get_platform_metrics()`'s `active_subscriptions` was `COUNT(*)` reused from the same query as the MRR `SUM`, both filtered `WHERE s.status IN ('ACTIVE','TRIAL')` — so 2 ACTIVE + 3 TRIAL subscriptions reported as "5 Active subscriptions" instead of 2. Verified live against real data before/after. Fix (migration `fix_active_subscriptions_kpi_excludes_trial`): decoupled `active_subscriptions` into its own `COUNT(*) WHERE status = 'ACTIVE'` query, independent of the MRR sum (which, at the time, still intentionally included TRIAL — later corrected in the MRR-specific fix directly above this entry). No RLS/auth changes; `super-admin.billing.tsx` and `super-admin.dashboard.tsx` both read the same RPC field, so both fixed by the one function change — no app code edited.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Claude           |
| 2026-08-11 | **Hostel Admin — added "Change Password" to the Account dropdown (Edit Profile already existed).** Audit found the Admin's Account menu (shared `Topbar.tsx`) had only "Edit profile" (a dialog, `EditProfileDialog.tsx` — full name/phone editable, email correctly read-only, pre-filled, Zod-validated via `profileUpdateSchema`, loading/disable-while-saving, success/error toast, Cancel/close-without-save all already correct) + "Sign out"; there was no Change Password option for Admin anywhere (unlike Warden/Accountant, which already had one via the role-agnostic `ChangePasswordForm`). Rather than duplicate that form, added a thin `ChangePasswordDialog` wrapper around the **existing, unmodified** `ChangePasswordForm` (current-password re-auth via `signInWithPassword`, new/confirm validated by the existing `changePasswordSchema`, `supabase.auth.updateUser`) and wired it into `Topbar.tsx` behind a new `isAdmin` (`resolved?.role === "HOSTEL_ADMIN"`) branch — added _alongside_ the existing `isAccountant` branch, so Super Admin's menu (which shares the same generic `else` fallback Admin used before) is byte-for-byte unchanged; only Admin gets the new Edit Profile + Change Password + Sign Out three-item menu. No new routes, no new schema, no DB/RLS changes — reused `profiles` table, `changePasswordSchema`, and Supabase Auth exactly as Warden/Accountant already do. `tsc --noEmit` (14 pre-existing unrelated errors, unchanged baseline), `eslint` (clean after `--fix`), `bun run build` all passed; dev-server smoke test on `/admin/staff` returned 200.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Claude           |
| 2026-08-10 | **Added: Super Admin → Tenants → "Assign Hostel Admin" — the one missing piece of tenant-lifecycle management the PRD requires but had no UI for.** Investigated first (per instructions): the self-serve signup flow (`fn_provision_tenant`, untouched) already makes the signup creator a tenant's first HOSTEL_ADMIN, but there was no way to _add/change_ one from the platform side. Found `role_assignments`/`tenant_memberships` write RLS policies (added in `20260729103833_...`) only permit "HOSTEL_ADMIN of that same tenant" or "self" — deliberately no `is_super_admin()` bypass — so a raw client-side insert from Super Admin would be rejected by RLS; this is exactly why the task required a secure Postgres/Edge function rather than a direct mutation. Added `fn_assign_hostel_admin(tenant_id, user_id, property_id)` (SECURITY DEFINER, migration `add_fn_assign_hostel_admin`) following the exact same established pattern as `fn_approve_refund`/`fn_end_support_session`: does its own internal `is_super_admin(auth.uid())` check (hard DB-layer rejection, not just app-layer), validates tenant/user/property server-side rather than trusting the caller, rejects a property that doesn't belong to the target tenant (cross-tenant guard), reactivates an existing revoked HOSTEL_ADMIN row for the same user+tenant+property scope instead of duplicating, ensures an ACTIVE `tenant_memberships` row (same as the existing Admin→Staff invite flow already does for Warden/Accountant), and writes one `audit_logs` row in the same transaction. Verified all three paths live against real data before/after, wrapped in explicit `BEGIN…ROLLBACK` so nothing was permanently mutated: non-Super-Admin caller → rejected; cross-tenant property → rejected; valid Super Admin call → `is_hostel_admin()` flips true for the correct property and the audit row is written. Added 3 new TanStack server fns to `src/lib/super-admin.functions.ts`: `getTenantHostelAdmins` (read current admin(s) for display), `listTenantMembers` (tenant-scoped user picker — deliberately separate from the existing global `searchUsers`, since `tenant_memberships` has no Super-Admin-inclusive SELECT policy and reading it needed the same service-role-lookup pattern `listStaff` already established, not an RLS change), `assignHostelAdmin` (thin wrapper calling the RPC). UI: added an "Assign Hostel Admin" button + dialog to `super-admin.tenants.tsx`, directly alongside the existing "Subscription" button — same dialog-from-list-row pattern as `AssignSubscriptionDialog`, no new "Tenant Details" page invented. Property picker reuses the already-Super-Admin-inclusive `properties_select_scoped` RLS policy via a direct client read (same pattern `PropertySwitcher` already uses). Added RLS/RBAC test section 12 to `supabase/tests/rls_tests.sql` covering all three cases using existing seeded fixture IDs (no new seed data). No duplicate role system, no duplicate invite/search service, no RLS policy changes — the only DB change is the one new SECURITY DEFINER function. `tsc --noEmit` (14 pre-existing unrelated errors, unchanged baseline), `eslint` (clean after `--fix`), `bun run build` all passed; `get_advisors` shows exactly the expected two new findings for `fn_assign_hostel_admin` (anon/authenticated EXECUTE grant warnings), identical in kind to every other existing SECURITY DEFINER function in this codebase — not a regression. Dev-server smoke test on `/super-admin/tenants` returned 200.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Claude           |
| 2026-08-10 | **Fixed: Accountant Dashboard/Invoices never showed Admin-generated invoices, always stuck on "Choose a property first."** Root cause: every Accountant page read `usePropertyStore((s) => s.activePropertyId)` — a Zustand store that is _only_ ever populated by `PropertySwitcher`, which is Hostel-Admin-only UI (`admin.tsx` passes `showPropertySwitcher`; `accountant.tsx` never did). So `activePropertyId` stayed `null` forever for every Accountant, regardless of their real, RLS-scoped `role_assignments.property_id`. The actual fix-at-the-common-source (not per-page): added `useAccountantProperty()` in `src/lib/staff-scope.ts`, wrapping the _already-existing_ `useMyStaffProperty()` hook — the exact same one Warden pages have used successfully all along, and which is role-agnostic by construction (no `.eq("role", ...)` filter, just `user_id = auth.uid()` via the existing `"own role_assignments readable"` RLS policy). Swapped `usePropertyStore` → `useAccountantProperty()` in all 7 Accountant pages (dashboard, invoices, payments, refunds, reports, fee-plans, deposit-ledger) — zero new queries, zero RLS changes, zero duplicate invoice-fetching logic; they already called the same `listPropertyInvoices`/`getAgingReport`/etc. as before, just with a real `propertyId` now. Verified against real live data: the actual Accountant test account (`simplei6172@gmail.com`) resolves via this hook to `property_id = fd32ddaf-...` — the exact property containing the four named students (Vikrant, Prince Patel, Vikrant Patel, rajkumar Prajpati) and their 4 real invoices (₹78,000 paid, ₹0 outstanding) already visible in Admin → Finance → Invoices. Added the requested finance-only Student Lookup (`accountant.students.tsx` + new `StudentFinancePanel`) — a minimal student picker (name/admission#/room only, explicitly not the full Admin student-management module) that, on selecting a student, reuses the **already-existing** `InvoiceTable` and `InvoiceDetailDialog` components for that student's invoice history — no second invoice-list/detail UI built. Extracted `useReceiptDownload()` out of `PaymentHistoryPanel` (was a private local mutation) into a shared, exported hook so `InvoiceDetailDialog`'s payment rows could get the same receipt-download button without duplicating the signed-URL logic. **Also recovered**, via `git stash`, the entire Accountant Dashboard-completion and Accountant Profile work from two earlier sessions — both had been silently discarded from the working tree by an external `git reset`/checkout event (the same failure mode documented in the two prior recovery entries in this changelog) between when they were built and this session starting; found and reapplied via `git stash apply` against the two backup stashes ("main untracked files backup", "main local changes backup"), resolving one incidental merge conflict in `admin.finance.fee-plans.tsx` by removing an orphaned, disconnected `toFormInput`/`FeePlanRow` fragment (referenced a `FeePlanFormInput` type that wasn't imported in this branch's version, and wasn't called from anywhere in the file — looked like a separate, unrelated in-progress "Edit Fee Plan" attempt from elsewhere that never got wired up; safe to remove since it was dead code, not functionality). No DB/RLS changes this task. `tsc --noEmit` (14 pre-existing unrelated errors, unchanged baseline), `eslint` (clean after `--fix`), `bun run build` all passed; `get_advisors` unchanged; dev-server smoke test returned 200 for all 8 Accountant routes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Claude           |
| 2026-08-10 | **Fixed: Admin → Staff → Add Accountant/Warden gave immediate active access on invite, even to a non-existent email.** Root cause: `inviteStaff` (`src/lib/admin-staff.functions.ts`) set `role_assignments.is_active = true` and `tenant_memberships.status = 'ACTIVE'` synchronously at invite time — a later change had short-circuited the already-existing, already-wired-up "activate on first real sign-in" flow (`activateMyInvites()`, called from both `LoginForm.tsx` after password sign-in and `verify-otp.tsx` after OTP verification) with a comment arguing there was "no prove-you-own-this-contact step to wait for." Fix: restored the original intent — `inviteStaff` now creates the assignment as PENDING (`is_active: false`, membership `status: 'INVITED'`) and lets the pre-existing `activateMyInvites()` flip it to ACTIVE on the invitee's first successful login, which is the actual proof of contact ownership (a bounced email can never complete a password-reset + sign-in). Zero RLS/RoleGuard changes needed — every access-control helper (`is_finance_staff`, `has_tenant_role`, `get_user_role`) already gated on `role_assignments.is_active`, so a pending invite was already structurally unable to reach any staff route; it just never stayed pending long enough to prove it. Verified live against real DB data: a genuinely pending row (found already in the table) resolves to `get_user_role() = NULL`. Added duplicate handling: an already-ACTIVE assignment for the same person+role+tenant now throws a clear error instead of inserting a second row; an already-PENDING one now resends the invite notification instead of creating a duplicate (extracted the notification-sending block into a shared `sendStaffInviteNotification` helper, reused by both the resend-on-duplicate path and the new `resendStaffInvite` server fn). Frontend (`admin.staff.tsx`): pending rows now show "Resend invitation" + "Cancel invitation" instead of Edit + Revoke (Cancel reuses the existing `revokeStaff` mutation — a cancelled pending invite and a revoked active one both end up `is_active:false` + `revoked_at` set, which `StaffStatusBadge` already rendered correctly as "Revoked" before this change, so no badge-logic change was needed). Email-format validation was already enforced (`z.string().email()` in the invite schema) — no change there; per the task's explicit instruction, no SMTP/mailbox-existence probing was added — non-existence is caught structurally (the invite just never gets accepted) rather than by an unreliable pre-check. `tsc --noEmit`, `eslint` (clean after `--fix` normalized formatting on the two substantially-rewritten files), `bun run build` all passed; dev-server smoke check on `/admin/staff` returned 200; `get_advisors` unchanged (no schema/RLS touched). This fix (and the two below it) was later found reverted from the working tree by an external checkout/reset event — recovered from `git stash` and reapplied; see the 2026-08-10 "recovered" entry further down for the recovery details.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Claude           |
| 2026-08-10 | **Follow-up fix: remaining `'TRIALING'` vs `'TRIAL'` status-string bug (flagged but deliberately left out-of-scope in the prior dashboard audit).** Queried live DB objects directly (not just migration files, since those are historical/superseded) and confirmed all three flagged locations still had the bug: `fn_effective_feature()` (feature-flag resolution), and RLS policies `plans_read_scoped`/`plan_features_read_scoped`. A codebase-wide sweep (`grep -rn TRIALING`) turned up one more live instance not previously flagged: `listTenantFeatures()` in `src/lib/super-admin.functions.ts` (used by the Feature Flags admin page to find a tenant's current plan) — same bug pattern, directly affects Feature Flags functionality, so fixed as part of the same bug rather than left for later. Fixed via migration `fix_trialing_status_bug_feature_flags_and_rls` (recreates the function + both policies with `'TRIAL'`, no other logic/scope changes) plus a one-line TS fix. Verified: zero live `TRIALING` references remain anywhere in the DB (`pg_proc`/`pg_policies`/`information_schema.views` swept) or in `src/`; confirmed the fixed status-matching logic (`IN ('ACTIVE','TRIAL')`) produces the intended ACTIVE/TRIAL-get-plan-features, PAST_DUE/PAUSED/CANCELLED-excluded behavior via direct truth-table query (no PAST_DUE/PAUSED/CANCELLED subscriptions exist live to test end-to-end, and `plan_features` has zero seeded rows in this environment — both are pre-existing data gaps, not something this fix could or should address). No RLS scope/business-logic changes beyond the status-string correction; `get_advisors` unchanged from baseline. `tsc --noEmit` and `bun run build` clean; dev-server smoke checks on `/super-admin/dashboard` and `/super-admin/feature-flags` both 200. Deliberately left the pre-existing whole-file CRLF `prettier/prettier` noise in `super-admin.functions.ts` untouched (confirmed via `git diff --stat` that this fix's diff is exactly 1 line) to keep the change minimal and focused, per this task's explicit "no unrelated changes" instruction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Claude           |
| 2026-08-10 | **Accountant Account/Profile section — audit + fill-in-gaps.** Audit found the Accountant's account menu (in the shared `Topbar.tsx`, used by all `DesktopShell` roles) only had "Edit profile" (a dialog, `EditProfileDialog.tsx` — full name/phone editable, email correctly read-only, no photo) + "Sign out"; there was no "My Profile" view and no "Change Password" option at all — the user's claim they already existed didn't match Accountant's actual menu (that fuller My Profile/Edit/Change-Password/Logout set exists only for Warden's separate `MobileHeader`). Built the missing pieces, scoped to Accountant only: added role-conditional branching in `Topbar.tsx` (`isAccountant` check, mirroring the existing `isWarden` pattern in `MobileHeader.tsx`) so only Accountant gets the expanded 4-item menu — Admin/Super Admin's menu is byte-for-byte unchanged. Added `accountant.profile.index.tsx` (My Profile: avatar, full name, phone, read-only email, read-only Role/Assigned Property sourced from `role_assignments`), `accountant.profile.edit.tsx` (Full Name + Phone required via new minimal `accountantProfileEditSchema` in `schemas/profile.ts`, Email always read-only — no auth email-change flow introduced, per instruction — plus optional avatar upload/remove reusing the existing `avatars` storage bucket, the same mechanism Warden's edit page already uses), and `accountant.profile.change-password.tsx`. Extracted the Warden change-password page's body (which was already 100% role-agnostic — plain `supabase.auth` calls) into a shared `ChangePasswordForm` component instead of duplicating the auth flow; refactored `warden.profile.change-password.tsx` to use it with zero behavior change. Deliberately did not add DOB/gender/address/emergency-contact/blood-group fields (explicitly out of scope — "finance/staff account, keep minimal") and did not touch dashboard/invoices/payments/refunds/reports/routes/RLS. No DB/migration changes — reused the existing `profiles` table columns and `avatars` bucket only. `tsc --noEmit`, `eslint` (clean after `--fix`), `bun run build` all passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Claude           |
| 2026-08-10 | **Accountant role — audit + fill-in-gaps pass (no redesign, no duplicate systems).** Audit found the finance backend (`src/lib/finance.functions.ts`, `fee_plans`/`invoices`/`payments`/`refunds`/`deposit_ledger_entries` tables, `is_finance_staff` RLS covering ACCOUNTANT+HOSTEL_ADMIN, `fn_approve_refund` maker-checker RPC, `generate-receipt` Edge Function) was already solid — only the Accountant-facing UI had gaps. Fixed the hardcoded dashboard (`accountant.dashboard.tsx` showed literal `"—"`/`0`) by wiring it to the existing `getAgingReport` server fn (same one `AgingReportPanel` already used) for real Collected/Outstanding/Overdue numbers. Added the missing Fee Plans route for Accountant by extracting the existing Admin fee-plans page's query+list into a shared `FeePlansPanel` component (RLS already allowed ACCOUNTANT via `is_finance_staff`, only the route was missing — Admin's page now uses the same panel, zero behavior change there). Added invoice search/status-filter/detail-dialog (new `InvoiceDetailDialog`, shows GST fields since the invoice detail view had never surfaced `gst_invoice`/`seller_gstin_snapshot`/`buyer_gstin_snapshot`/`tax_paise` anywhere despite existing on the table). Added payment history + receipt download (`PaymentHistoryPanel`, signs a short-lived URL against the existing private `receipts` storage bucket + `documents` row the Edge Function already writes) since `PaymentEntryForm` was entry-only with no way to see past payments. Added refund history/status (`RefundHistoryPanel`) since `RefundRequestForm` invalidated a `["refunds"]` query key that nothing on the page ever rendered. Added a read-only Deposit Ledger view (`DepositLedgerPanel`, student picker → ledger entries + `previewMoveOut` reused for the provisional-refund figure) — deliberately does **not** expose the move-out trigger action itself (`moveOutAllocation`), which stays Admin-only property-lifecycle territory per PRD's Accountant scope boundary. Deliberately **not implemented**: Discount/Waiver workflow — schema only has a bare `invoices.discount_paise` column with no lifecycle/approval table, and CLAUDE.md's money rules forbid silently editing issued invoices (adjustment/reissue only), so a correct implementation needs new schema design under maker-checker; rushing it in this pass risked violating that rule and Step 13's "no unnecessary new tables" instruction — flagged for separately-scoped work instead. No DB/RLS/migration changes; `get_advisors` output unchanged from baseline. `tsc --noEmit`, `eslint` (clean after `--fix` normalized line endings), `bun run build` all passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Claude           |
| 2026-08-13 | **Monthly Attendance Report implemented (Admin/Warden/Student/Parent), replacing the Phase-11 stub.** `AttendanceReportPanel` in `src/components/reports/panels.tsx` was a hardcoded `EmptyState` stub since Phase 9 reports were built (attendance didn't exist yet); now a real per-student monthly roll-up with a month picker, summary KPIs (students marked / present / absent / avg attendance %), a bar chart (`AttendanceChart`, new, `src/components/reports/charts.tsx`, mirrors `SlaComplianceChart`'s tone-bucket coloring), and CSV/PDF export via the existing `ExportButton`/`csv.ts`/`pdf.ts` utilities — same composition every other report panel already uses, no new export mechanism. Backing data: new view `v_attendance_monthly_summary` (migration `20260813120000_attendance_monthly_summary_view.sql`, `security_invoker = true`, same convention as `v_occupancy_summary`/`v_invoice_aging`/`v_complaint_sla_summary`) grouping `attendance` by tenant/property/block/student/calendar-month into present/absent/late/leave/out-pass/marked-day counts + attendance %; new server fn `getAttendanceReport` (`src/lib/reports.functions.ts`) reads it filtered by `property_id`+`month`, then name-joins `students` the same way `getSlaComplianceReport` name-joins categories/wardens. **Scope, unchanged from existing RLS (no new policies needed):** Admin gets property-wide (`is_hostel_admin`), Warden gets their assigned property/block (`att_warden_write` is `FOR ALL`, already gating SELECT — confirmed attendance was already block-scoped for Warden reads, unlike most other tables where Warden reads are property-wide only) — wired into `admin.reports.tsx` (full, with export) and `warden.reports.tsx` (`showExport={false}`, matching the existing Occupancy/SLA panels' Warden treatment); `accountant.reports.tsx` deliberately left untouched — Accountant has no RLS policy on `attendance` at all (finance-focused only, confirmed via migration grep), so no UI was added there. Student/Parent got a new shared component `MonthlyAttendanceReport` (`src/components/parent/MonthlyAttendanceReport.tsx`, follows the existing `AttendanceHistoryList` co-location precedent) with the same month-picker + summary + full daily-detail export, added _alongside_ (not replacing) each page's existing "last 30 days" `AttendanceHistoryList` — reads `attendance` and the new view directly via the user's own session (`att_student_read`/`att_parent_read` already scope rows to self/linked-child-with-`can_view_attendance`), no server fn needed, matching the codebase's "simple RLS-protected reads go direct" convention; export shown per PRD §7's "Data export (student PII): Student X (self), Parent X (linked child)" row. **Not yet applied to the live DB** — the new migration is written but not run (no local Supabase CLI in this environment and the Supabase MCP server needs interactive re-auth); needs `supabase db push` (or MCP once authorized) before the report will return data instead of erroring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Claude           |
| 2026-08-13 | **Warden Student Profile — expanded per PRD 7 "Student profiles: VE (assigned block)".** `warden.students.$id.tsx` was a 4-field stub (name/admission#/phone + `GuardianCard`); now shows personal info (email/DOB/gender/minor), academic info (institute/course/year), guardian/parent (existing `GuardianCard`, unchanged), current room/bed (reusing the same allocation+bed/room/floor/block join pattern as `student.profile.tsx`, scoped to _current_ open allocation only — not the Admin page's full history, matching the narrower Warden scope), and KYC/documents (existing `KycStatus` badge + a read-only per-document list; verification itself stays on the Warden Daily Brief, not duplicated here). Added editing via a new `StudentProfileEditDialog` component and `updateStudentProfile` server fn (`student.functions.ts`) reusing the existing `studentSelfProfileSchema` field set (extended with `student_id` as `studentStaffProfileEditSchema` in `schemas/student.ts`) — same fields the student can self-edit (name/phone/email/DOB/gender/academic info), no finance/status/delete/move-out fields exposed. Scope enforcement: `students warden update` RLS only checks property (passes `_block_id: NULL`, see 20260715120431 migration), so per PRD 7.1 "server-side is the primary guard" the new server fn resolves the student's _current_ block via their open allocation (`allocations.block_id`, matching `uidx_allocations_active_student`'s status set) and re-checks `warden_can_write_scope` against it — same pattern `updateGuardianPhone` already uses for property-only scope. No RLS/migration changes (existing policies already covered every read/write this needed); no delete, move-out, confirm-admission, or finance actions added (deliberately excluded, Admin-only per the matrix). Audit-logged via `supabaseAdmin` insert (`STUDENT_PROFILE_UPDATED`), matching the `updateGuardianPhone` pattern. `tsc --noEmit` and `eslint` clean on all changed/new files (only pre-existing repo-wide CRLF/prettier noise and one pre-existing `_block_id: null` RPC-arg type mismatch already present identically in `guardian.functions.ts:47`, left as-is for consistency rather than one-off patched).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Claude           |
| 2026-08-08 | **Warden Gate module — Step 5: final QA pass, code-inspection only (no camera/live browser available in this environment).** Traced every mutation's invalidation path (`decideMut`→`["gate-passes"]`, `scanMut`→`["gate-events"]`+`["gate-passes"]`, `visMut`→`["visitors"]`+`["gate-events"]`, `registerMut`→`["visitors"]`) against the KPI cards' data sources and confirmed all 5 KPIs recompute correctly after approve/reject/check-in/check-out/register — all real, no mock values. Confirmed QR payload compatibility by reading `student.gate-pass.tsx` (`JSON.stringify({id: pass.id, t})`, untouched all session) against `CameraQrScanner.tsx`'s decode (`payload.id`/`payload.t`) — format matches. Confirmed `scanGatePass`/`checkVisitor` duplicate-event prevention is real: `gate_events` has a DB-level `UNIQUE (tenant_id, idempotency_key)` constraint (pre-existing migration, unchanged) and both server fns swallow only the resulting duplicate-key error. Confirmed RoleGuard: `warden.gate.tsx` has no route guard of its own because it's a TanStack Router dot-segment child of `warden.tsx`, whose layout wraps `<MobileShell allow={["WARDEN"]}>` (→ `RoleGuard`) around the shared `<Outlet/>` — Gate is protected, same as every other warden.* route. Confirmed no RLS/migration files changed this session (`git status supabase/migrations/` unchanged) and `get_advisors` output is identical to Steps 3/4's baseline. **One real bug found and fixed:** `RegisterVisitorDialog` was unconditionally rendered in `warden.gate.tsx`, so its `useStudentsInProperty(propertyId)` query fired on every Gate page load regardless of whether the Register dialog was ever opened — wasteful, unnecessary fetch. Fixed by gating the dialog behind `{registerOpen && <RegisterVisitorDialog .../>}`, mirroring the already-established `{detailsPass && <ApprovalDetailsDialog/>}` pattern in the same file — dialog local state now also naturally resets on close via unmount instead of the explicit `reset()` call, no behavior change for the user. **One known gap noted, not fixed (out of scope — would be new UI, not a bug):** the Reject button never collects a reason even though `decideGatePass`'s `reason` field is wired end-to-end server-side (`gate_pass_approvals.reason`, `gate_passes.decision_reason`) — `reason` is `.optional()` in the schema so nothing is broken, but a warden currently has no way to type one in this UI. `tsc --noEmit`, `eslint` (both clean), `bun run build`, and a dev-server smoke request to `/warden/gate` (200 OK) all passed after the fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Claude           |
| 2026-08-08 | **Warden Gate module — Step 4: UI/UX cleanup pass only, no functionality/data/RLS changes.** Fixed six genuine layout issues in `warden.gate.tsx`: (1) the 5-card KPI grid left a lone orphan card on 2-column mobile — the 5th card now spans full width via a wrapping `col-span-2 sm:col-span-1` div, same trick used nowhere else yet but consistent with the existing `KpiCard` grid pattern from Daily Brief. (2) the 4-tab `TabsList` (one label dynamically widened by a count, e.g. "Approvals (12)") is `inline-flex`/`whitespace-nowrap` with no wrap or scroll affordance — measured total content width exceeds a ~375px viewport after padding, so wrapped it in an `overflow-x-auto` container (matches the plain, no-new-component fix already used elsewhere for horizontal overflow). (3) Approval and Visitor status badges both always rendered `variant="secondary"` regardless of actual status (Pending/Approved/Rejected/Expired all looked identical) — added a small `statusBadgeClass()` helper reusing the exact tone classes `StepBadge` (same file) already established (`success`/`warning`/`destructive`/`muted` at `/15` opacity), applied to both badges via the existing `Badge` `outline` variant + `cn()` override — no new component. (4)+(5) Approval card and Visitor card action rows were a single non-wrapping `flex justify-between` — on narrow screens the 2-3 action buttons could squeeze/overflow past the text block; added `flex-wrap`, `min-w-0 flex-1` on the text column, `shrink-0` on the button group, and `truncate` on long name lines — same defensive pattern already used by `ComplaintCard`. (6) Live Feed rows had the same non-wrapping issue between the badge+name and the full date/time string; added `flex-wrap`, `min-w-0`/`truncate` on the name, `shrink-0` on badges/timestamp. After hand-editing, ran `eslint --fix` on the file to clear pre-existing (not this-step-introduced, but now caught since previous runs over-filtered all `prettier/prettier` output including legitimate line-wrap violations, not just the known CRLF noise) formatting-only violations — zero logic changed by the auto-fix, confirmed via `tsc --noEmit` + `bun run build` passing before and after. No DB/RLS/query/server-fn changes; no new dependencies; Emergency Access still not implemented (out of scope). `tsc --noEmit`, `eslint` (file now fully clean), `bun run build`, and a dev-server smoke request to `/warden/gate` (200 OK, no error-boundary text) all verified.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Claude           |
| 2026-08-08 | **Warden Gate module — Step 3: Visitor Registration, Search, Filters (the last "missing" items from the original audit; Emergency Access still deliberately out of scope).** Visitor Registration: `visitors` table already had a `vis_warden` RLS policy that is `FOR ALL` (not just read) via `warden_can_read_property` — no RLS change needed, only the missing server function + form. Added `createVisitor` server fn (`src/lib/operations.functions.ts`), same shape as neighboring `createGatePass`: validates `{host_student_id, name, phone, purpose, expected_at?}`, all pre-existing columns; `status` left off the insert so the column default (`REQUESTED`) applies, reusing the existing lifecycle `checkVisitor` already drives forward. Deliberately did not insert a `gate_events` row on registration — checked the table's CHECK constraints (`direction` must be IN/OUT, exactly one subject required) and a brand-new `REQUESTED` visitor is neither, so the existing event model genuinely doesn't support a "registered" event; first real gate activity still flows through the untouched `checkVisitor` → `gate_events` path. Also deliberately excluded `photo_document_id`/`id_document_id` from the form — real columns, but wiring them needs a full document-upload flow (storage bucket + `registerDocument`, mirroring `KycUploadForm.tsx`), a materially bigger addition than "minimum required." New `RegisterVisitorDialog` in `warden.gate.tsx` reuses `useStudentsInProperty` (already used elsewhere in the file) for the host picker; on success invalidates the existing `["visitors"]` query key, toasts, closes. Search: client-side filters only (no debounce — filtering an already-fetched ≤100-row array on keystroke needs no network rate-limiting) — Visitors by name/host/room/phone, Approvals by name/room (required additively extending `useGatePasses`'s `select()` to embed room via the student's active allocation, same non-breaking pattern used repeatedly all session), Live Feed by name (already embedded). Filters: Visitors toggle extended from 3 to 4 buttons (+ Checked Out); Approvals gained a Pending/Approved/Rejected/Expired `Select` driving a **second**, additional `useGatePasses` call for the tab's list — the original fixed "pending" call was left untouched since it still feeds the KPI card and tab badge count (React Query dedupes the fetch when the filter is on its default "Pending" state, since the key matches); Live Feed gained All/Entry/Exit/Alerts. No arbitrary date-range picker added — the existing "Expected Today" toggle already is the date filter the brief asked for, and anything more would be the "unnecessary filter" it warned against. No DB/RLS changes this step. `tsc --noEmit`, `eslint` (scoped), and `bun run build` all clean; `get_advisors` shows only pre-existing findings unrelated to gate/visitors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Claude           |
| 2026-08-08 | **Warden Gate module — completed the "incomplete/broken" items from the prior audit.** Real camera QR scanner (`src/components/gate/CameraQrScanner.tsx`, new — the only new npm dep this pass, `jsqr` for frame decoding, ~233KB/56KB gzip, confirmed via `bun run build`) replacing the "coming soon" stub; decodes the exact `{id, t}` JSON format `student.gate-pass.tsx` already encodes into its QR, then calls the _same_ `scanGatePass` mutation the manual Pass ID/Token form already used (manual entry untouched, still works as fallback) — validation states (invalid/expired/already-used/denied) are pure client-side labels over `scanGatePass`'s existing thrown messages, no parallel validation logic. Approval Details dialog added (destination, Parent→Warden approval chain from existing `gate_passes` columns, and — first-ever reader of `gate_pass_approvals`, which has been written on every decision since it was built but never displayed — a chronological history list, approver names resolved via a second `profiles` lookup since `approver_user_id` has no FK for an embed). Visitors tab gained an All/Expected-Today/Inside toggle over the _same_ already-fetched `useVisitors` data (its `select()` only additively extended to embed room number through the host's allocation — same non-breaking pattern as every prior enrichment). Gate KPI row (5 `KpiCard`s, `bareIcon`, reusing the Daily Brief pattern) computed entirely from data the page already fetches — zero new queries. Empty states switched to the existing `EmptyState` component; loading states added via the existing `Skeleton`; error states via existing `Alert` + a `getErrorMessage()`-backed retry button. Explicitly left untouched per instruction: visitor registration, search, filters, emergency access — plus every item on the "already works" list (approve/reject, manual QR validation, currently-out list, check-in/out, live feed, curfew detection, parent-approval chain, realtime, RLS, student-side request+QR) — none of their underlying hooks/server-functions were modified. No DB changes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Claude           |
| 2026-08-08 | **Fixed pre-existing crash: Warden Gate page threw "This page didn't load."** User supplied the actual browser console trace: `Error: cannot add postgres_changes callbacks for realtime:gp-<propertyId> after subscribe() — at ops.ts:72`. Root cause, unrelated to any of the day's UI work: `warden.gate.tsx` calls `useGatePasses(propertyId, ...)` **twice** in the same component (once for the "pending" approvals list, once for "active"/currently-out passes), and `useGatePasses`'s realtime subscription (`src/lib/ops.ts`) named its channel `gp-${propertyId}` — identical for both calls. Supabase's JS client dedupes channels by name, so the second `.on(...)` call landed on the first call's already-`.subscribe()`d channel instance and threw, which the root error boundary caught and rendered as the generic "didn't load" page. Fixed by mixing a React `useId()` per-hook-instance value into the channel name (`gp-${propertyId}-${instanceId}`), guaranteeing every mounted instance of the hook gets its own channel regardless of how many times or with what filters it's called concurrently — the same latent naming pattern exists on a couple of other realtime hooks in this file but isn't currently double-invoked anywhere, so left alone per the reported-bug scope.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Claude           |
| 2026-08-08 | **Warden Daily Brief KPI icons — switched the title/icon row from flexbox to CSS Grid.** Third report of the same "Pending Complaints"/"Pending Gate Pass" icon-collides-with-title bug, specifically on the two longest, most wrap-prone titles, despite the flex row already having `min-w-0`/`flex-1`/`shrink-0`/`gap-3` (textbook-correct flexbox that shouldn't overlap). Rather than keep tweaking flex values blind, switched `KpiCard`'s `bareIcon` title/icon row (`src/components/dashboard/KpiCard.tsx`) to `grid grid-cols-[minmax(0,1fr)_auto] items-start gap-2` — the exact pattern `PageHeader.tsx` already uses successfully elsewhere in this codebase for the identical "title + fixed trailing element, never overlap" problem. Applies uniformly to all 6 bareIcon KPI cards (visually identical output for the 4 that were never reported broken — grid and flex render the same when there's no overflow, so this isn't a visible change for Total Students/Present/Absent/Visitors Today, just a shared more-robust layout underneath).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Claude           |
| 2026-08-08 | **Warden Daily Brief KPI cards — removed boxed icon containers entirely.** User's follow-up screenshot showed icons still overlapping/reading as floating circular badges even after the tone-color fix (previous entry below) — the boxed `h-10 w-10 rounded-xl` icon container was itself the problem, not just its color. `KpiCard` (`src/components/dashboard/KpiCard.tsx`) got a new `bareIcon?: boolean` prop (default `false`, preserving the boxed look exactly as-is for the 4 other dashboards using this shared component — admin/accountant/super-admin/billing were never asked to change) — when `true`, renders a plain small `<Icon>` with only a text-color class (new `toneTextClasses` map in `src/lib/utils.ts`, sibling to the existing `toneClasses` bg+text map used elsewhere) and no wrapper div, background, border, or shadow at all. Daily Brief's 6 KPI cards now pass `bareIcon`; card min-height dropped to `92px` (no longer needs to fit a 40px icon box) and the grid's 6-column breakpoint moved from `xl` to `lg` so 6-across triggers on more desktop widths. Quick Actions (which intentionally keep icon-in-colored-box, per explicit "do not change" instruction) still use the original `toneClasses` boxed style unchanged.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Claude           |
| 2026-08-08 | **Warden Daily Brief — visual polish pass against a reference screenshot.** Fixed the "yellow icon overlapping title" bug reported after the initial build-out (see the entry below): `KpiCard` (`src/components/dashboard/KpiCard.tsx`) hardcoded `bg-primary/10 text-primary` for every icon, and since dark mode's `--primary` is gold, all 6 KPI icons rendered identically amber regardless of meaning. Added a `tone?: SemanticTone` prop (`"primary" \| "success" \| "destructive" \| "warning" \| "info" \| "muted"`, defaults to `"primary"` so the other 4 dashboards using `KpiCard` — admin/accountant/super-admin/billing — are visually unaffected) backed by a new shared `toneClasses` map in `src/lib/utils.ts`, reusing the app's existing `--success`/`--warning`/`--destructive`/`--info` tokens (confirmed already correctly defined for both themes in `src/styles.css`, including light mode's forest-green/amber/red/steel-blue — no new tokens or hex values needed). Daily Brief now assigns tones semantically: Present=success, Absent=destructive, Pending Complaints/Pending Gate Pass=warning, Visitors Today=info, Total Students=muted — matching the design system's documented rule (Design.md Sec 4.2) that status colors stay disjoint from the brand gold. Quick Actions rewired from plain outline buttons into full-width horizontal rows (icon-in-tone-box + label + trailing chevron) matching the reference. Pending Complaints rows gained a priority dot + colored priority badge and an Open/View button pair (both navigate to `/warden/complaints` — no per-complaint deep-link exists, intentionally not built for this pass); both Pending Complaints and Pending Gate Pass gained a "View all…" footer link. Left Attendance Summary, Recent Activity, and all data-fetching/mutation logic untouched — this was a pure visual pass, confirmed via `bun x tsc --noEmit`/`eslint` scoped to the changed files and a dev-server module-compile check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Claude           |
| 2026-08-08 | **Warden Daily Brief dashboard — built out from a stub.** `warden.daily-brief.tsx` was a genuine 17-line placeholder (`PageHeader` + static `EmptyState`, zero queries) — confirmed via full-codebase search that none of the requested widgets existed anywhere before writing any code, so this was net-new, not a duplication risk. Built entirely by composing existing hooks with no new ones needed: `useStudentsInProperty`/`useAttendance`/`useComplaints`/`useGatePasses`/`useVisitors`/`useMessMenusForDate` (all already in `src/lib/ops.ts`/`src/lib/complaint.ts` from earlier sessions) plus the exact `decideGatePass` approve/reject mutation `warden.gate.tsx` already uses — the Pending Gate Pass widget on this page is a second call site of that same server function, not a reimplementation. Wired up `KpiCard` (`src/components/dashboard/KpiCard.tsx`), which existed but had never been used anywhere, for the 6 summary tiles. Key finding: neither `audit_logs` (no client INSERT policy, nothing writes attendance/complaint/gate-pass/mess events into it — only 2 unrelated super-admin writes exist) nor `notifications` (all events are recipient-scoped to specific individuals, no `ATTENDANCE_SUBMITTED`/`GATE_PASS_APPROVED`/`MENU_PUBLISHED` event type exists) can back a "Recent Activity" feed — assembled it client-side instead from each domain table's own timestamp columns (`attendance.marked_at` grouped by batch, `complaints.updated_at`/`resolved_at`, `gate_passes.warden_approved_at` via one new small query for today's approvals, `mess_menus.published_at`), reusing data already fetched for the other widgets wherever possible (only `gate_passes` approved-today needed a dedicated query, since the existing `useGatePasses` call on this page is scoped to `PENDING_WARDEN` only). Attendance Summary auto-updates after a save on `/warden/attendance` for free — same `useAttendance(propertyId, date)` query key, same React Query cache, no extra wiring. No DB migration, no new hooks, no changes to any other route — pure composition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Claude           |
| 2026-08-07 | **Warden Attendance module — production-ready enhancement.** `warden.attendance.tsx` was a bulk-mark MVP: date input, "Mark all Present", a flat name list with a status `Select` (`PRESENT/ABSENT/ON_LEAVE/OUT_PASS`), no student ID/room/photo, no summary, no search/filter, no remarks. Found `attendance.notes` and the `UNIQUE(student_id, attendance_date, session)` duplicate-prevention constraint already existed and were already threaded through the existing `bulkMarkAttendance` server fn (`src/lib/operations.functions.ts`) — just never populated/enforced from the client. Real gap: **no `LATE` status existed** in the DB CHECK constraint (`PRESENT, ABSENT, ON_LEAVE, OUT_PASS, NOT_MARKED` only) — added via migration `add_attendance_late_status` (additive, dropped+recreated `attendance_status_check` to add `'LATE'`; `OUT_PASS`/`NOT_MARKED` stay in the constraint for other flows, just not offered as options in this UI — same DB-keeps-superset pattern as the Complaints status workflow). "Leave" maps to the existing `ON_LEAVE` value, no new value needed there. Added a `.superRefine` to `bulkAttendanceSchema` requiring non-empty `notes` for any non-Present entry (server-side backstop; client also blocks Save and highlights the offending row). Extended `useStudentsInProperty` (`src/lib/ops.ts`) to additively select `admission_number`, `photo_path`, and the active allocation's `rooms(room_number)`/`blocks(name)` — used only by this page's roster rendering (photo/ID/room/summary/search-by-room), other callers unaffected. Summary counts (Total/Present/Absent/Late/Leave) and search/status/room filters are derived client-side via `useMemo`/`useCallback` over the existing `overrides` local-state pattern, recalculating live before Save (no new queries). The "attendance history" requirement turned out to already be satisfied by the existing date-picker → `useAttendance` → "Saved: {status}" flow — extended that same badge to also surface the saved remark rather than building a separate history view, per "do not redesign." `students.photo_path` exists but nothing in the codebase populates it yet (no admission-photo upload pipeline wired to it) — resolved against the `avatars` bucket for consistency with the Complaints/Mess avatar pattern; will show initials fallback until something populates it, a pre-existing gap outside this task.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Claude           |
| 2026-08-07 | **Warden Complaint Management — production-ready enhancement.** `warden.complaints.tsx` was a minimal MVP (Start-work/Resolve buttons only, no category/student-info display, no search/filter, no comments). DB already had `category_id`/`priority`/`complaint_categories` fully wired but the warden page never surfaced them. Added: enriched `useComplaints()` select (`src/lib/complaint.ts`) joining `students(full_name, admission_number, profile_id, profiles(avatar_path))`, `rooms(room_number)`, `blocks(name)`, `complaint_categories(name)` — additive, non-breaking for the admin/parent/student pages sharing the same hook; `ComplaintCard` (shared component) now shows a category pill + student avatar/name/admission#/room/block line; a search box (complaint number/student name/room number) + status/priority/category filter `Select`s above the list, layered on top of the existing "my queue" scope; a "View Details" `Dialog` replacing the old inline buttons, with a status-workflow `Select` (current real status ∪ the 4 spec'd targets Open/In Progress/Resolved/Closed — DB keeps all 8 real statuses (`ASSIGNED`/`WAITING_FOR_STUDENT`/`REOPENED`/`CANCELLED` are load-bearing for `fn_complaint_auto_assign` and the student `RatingWidget` reopen flow, so the CHECK constraint was deliberately left untouched), Resolved requiring notes and capturing a new `resolved_by` column (migration `add_complaint_resolved_by_and_comments`) alongside the existing `resolved_at`. New `complaint_comments` table (own migration, mirrors the low-risk `mess_feedback`-style pattern rather than extending the parent↔warden `conversations`/`messages` tables, which would have meant touching the shared `can_access_conversation()` function) backs a per-complaint conversation thread (`ComplaintCommentThread.tsx`, shared between the Warden details dialog and a new collapsible "Show conversation" section on the Student complaints page, so Students can actually reply). Extracted a shared `getErrorMessage()` helper (`src/lib/utils.ts`) — Supabase/PostgREST errors aren't `instanceof Error`, a bug first hit and fixed in the Mess module — and backfilled `warden.mess.tsx` to use it too instead of its local duplicate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Claude           |
| 2026-08-07 | **Warden Mess module — production-ready enhancement.** `warden.mess.tsx` was a publish-only MVP (create menu, view aggregate feedback badge) with no edit/delete/headcount/feedback-detail/timing. Added, in the same file (no redesign, no new `components/mess/` folder — matches existing colocation convention): per-card Edit (`Dialog`, updates `mess_menus` + replaces `mess_menu_items` by delete-then-reinsert) and Delete (`AlertDialog`, single delete relies on existing `ON DELETE CASCADE` to clean up items/headcount/feedback); a clickable feedback badge opening a `Dialog` with per-student name/rating/comment/timestamp (joins `mess_feedback.student_id → students.full_name`), "No feedback available." when empty; inline headcount recording (Expected/Served, skipped auto-computed) wired to `mess_headcounts` — this table existed since the original Phase-11 migration but had zero read/write paths anywhere in the app; and a `serve_time` column (new migration `add_mess_menus_serve_time`) so each meal's own `mess_menus` row (already one row per property+date+meal) carries its own time, shown as "Breakfast Time"/"Lunch Time"/etc. per card. `useMessMenusForDate` (`src/lib/ops.ts`) now embeds `mess_headcounts(*)` alongside `mess_menu_items(*)` in one query. New `src/schemas/mess.ts` (`messMenuFormSchema`, `messHeadcountSchema`) shared by the publish form and the new edit dialog. No RLS changes needed — `mh_staff`/`mf_staff_read`/`mm_warden` policies already covered every read/write this added.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Claude           |
| 2026-08-07 | **Warden Profile Management** — replaced Warden's bare "Sign out" button with a role-aware dropdown (My Profile / Edit Profile / Change Password / Logout; other roles' `MobileHeader` unchanged). Applied the previously-uncommitted `add_staff_profile_fields` migration (it turned out to already be live in the DB under a slightly different filename/timestamp — reconciled the local file to match remote migration history) adding `profiles.gender/date_of_birth/blood_group/address/alternate_phone/emergency_contact_name/emergency_contact_number`, `role_assignments.employee_id` (auto-generated `WRD-XXXXXXXX` etc. via trigger), and a public `avatars` storage bucket with self-scoped RLS. Added a follow-up migration pinning `employee_id` immutable on every `UPDATE` (the existing self-update policy on `role_assignments` had no column restriction) plus a `search_path` hardening migration for both trigger functions. Regenerated `types.ts` (purely additive diff, verified no regressions). New routes: `warden.profile.index.tsx` (read-only view, read-only fields visually locked), `warden.profile.edit.tsx` (editable fields + avatar upload to the new bucket), `warden.profile.change-password.tsx` (re-verifies current password via `signInWithPassword` before `updateUser`, since Supabase doesn't check the old password itself). Hit the same class of routing bug as the 2026-07-29 QA pass below: naming the read-view file `warden.profile.tsx` made it an implicit parent layout for `warden.profile.edit.tsx`/`warden.profile.change-password.tsx` (TanStack Router's dot-segment convention), which would have swallowed the child routes with no `<Outlet />` — renamed to `warden.profile.index.tsx` to keep all three as siblings under `warden.tsx`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Claude           |
| 2026-07-29 | **First end-to-end QA pass on the Hostel Admin panel** (Playwright + Supabase MCP, 68 automated cases, 58 passing). Fixed two blocking bugs: (a) three parent routes (`admin.properties`, `admin.students`, `admin.students.$id`) had child routes but rendered no `<Outlet />`, so the property setup wizard, structure builder, student detail and move-out pages all silently rendered their parent's list instead — split each into a layout route + `.index.tsx` page, matching `admin.finance.tsx`; (b) `useMyNotifications` created its realtime channel inside an async `.then()`, leaking it on teardown and throwing ``cannot add `postgres_changes` callbacks … after `subscribe()` `` on every admin route — added a `cancelled` guard. Seeded a QA dataset (2 blocks, 24 beds, 20 students, 9 allocations, 27 invoices, 9 complaints, 126 attendance rows) into tenant `Suryavanshi Residency`. Discovered the whole product had never been driven past property creation — 0 beds/students/invoices existed project-wide, explained by (a). Full findings in the QA plan (15 findings, 9 known issues confirmed).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | QA pass          |
| 2026-07-15 | Context alignment pass across all 7 docs. Reversed JS→TS + Tailwind/shadcn (D1). Added Tenant level to PRD & Phases (D2). Click-wrap e-sign (D3). Created this file, dropped Test-Checklist (D4). Rewrote Design.md from the logo, AA-verified (D5). Deferred Redis with documented seams; added Postgres `checkRateLimit()` + `rate_limits` table (D6–D8). Purged 5 phantom tables from Phases (property_staff→role_assignments, guardian_links→student_guardians, feature_flags→plan_features/tenant_feature_overrides, feedback→feedback_surveys/responses, audit_log→audit_logs, organizations.subscription_status→subscriptions). Added Edge-function/pooler/Zod/RLS standing rules.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Alignment review |

---

## 6. Known issues / risks

- **Load test pending:** the "50k users on Supabase Pro" posture is a design target, not load-tested. A realistic concurrent-active load test is required before launch (`Architecture.md` Sec. 26.3, Phase 12).
- **RLS is documented, not yet proven:** every table needs the RLS test pairs in `Rules.md` Sec. 15.9 before touching real student data.
- **India data residency:** Supabase region + all third-party processors must be verified before production (`Architecture.md` Sec. 2.4).

### Open defects from the 2026-07-29 QA pass

Ordered by severity. Confirmed by automated test or direct DB query unless marked otherwise.

- **Phone OTP login is dead (Critical).** `sendPhoneOtp` calls `check_rate_limit` with the anon key, but migration `20260717074011` revokes EXECUTE on that function from `anon`. Reproduced: `Rate limit check failed: permission denied for function check_rate_limit`. Phone OTP is the _default_ login tab. (MSG91 integration in progress may supersede this — verify the grant either way.)
- **Column-guard triggers block backend writes (High).** `fn_is_acting_as_student_only()` is `NOT(is_super_admin(auth.uid()) OR has_any_tenant_role(auth.uid(), t))`, which is **true** when `auth.uid()` is NULL — i.e. every service-role, Edge Function and pg_cron context. All four triggers (`complaints`, `students`, `agreements`, `gate_passes`) reject those writes. Notably this would break `fn_scan_complaint_sla_breaches` even if it were scheduled. Fix: require `auth.uid() IS NOT NULL`.
- **`fn_scan_complaint_sla_breaches` is never scheduled (High).** Only `generate-invoices-daily` and `fee-reminders-daily` exist in `cron.job`. SLA breach flags can never set themselves.
- **`fn_generate_invoices` has no catch-up (High).** It only issues invoices when `EXTRACT(DAY FROM CURRENT_DATE) = COALESCE(billing_cycle_day, due_day)`. A single missed cron day silently skips that month's invoices, with no backfill and no detection.
- **Dashboard KPIs are hardcoded (High).** Occupancy/Collections/Open complaints/Active students render `0%`/`—`/`0`/`0` regardless of data, and the "Add your first property" empty state shows even with properties present.
- **Admin has no complaint actions (High).** The complaints board is read-only — no assign, resolve, comment or reopen.
- **Aging report disagrees with the revenue page (High).** `getAgingReport` includes VOID invoices and caps at 500 rows (aggregates computed over the capped set); `getRevenueCollectionsSummary` excludes VOID.
- **No mobile layout for the admin panel (High).** Sidebar is `hidden lg:flex` with no replacement nav below `lg`.
- **Fee plans cannot be edited or deleted, and `upsertFeePlan` always INSERTs (Medium)** — resubmitting duplicates the plan. Component amounts are entered in raw paise with no formatting.
- **Scheduled notices never publish (High)** — no scheduler exists; status stays `SCHEDULED` forever. DRAFT/SCHEDULED notices also never appear in "Recent notices", so their Cancel button is unreachable.
- **PARENTS notice audience fans out tenant-wide (High)**, not scoped to the property.
- **Structure dialog labels are not associated with inputs (Medium, a11y).** Block/floor/room dialogs use bare `<Label>` with no `htmlFor` — screen readers cannot announce the fields.
- **Student search passes wildcards raw into `ilike` (Medium)** — typing `%` matches every student. No debounce either.
- **Complaint block filter renders truncated raw UUIDs** instead of block names (Medium).
- **Duplicate property name fails silently (Medium)** — slug unique-constraint violation with no error toast.
- **Attendance report panel is a hardcoded stub (Medium)** — shows "activates once your hostel tracks attendance" despite 126 attendance rows existing.
- Not yet verified, carried from code review: non-idempotent `moveOutAllocation` (double deposit deduction), `createAllocation` check-then-insert race, unscoped `receipts` storage policy, refund self-_rejection_ allowed, `voidInvoice` with no status precondition, spoofable `x-forwarded-for` on the admission rate limit.

---

## 7. Pending / open questions

Carried from `PRD.md` Sec. 13 (product-level, unresolved):

1. Property size at which a solo Warden becomes insufficient (multiple Wardens per block vs per property).
2. Whether Accountant is a paid add-on seat.
3. Whether the gate-pass QR scan needs a dedicated low-privilege "front desk" login vs shared-device Warden login.
4. Whether parent SSO also accepts email magic-link or stays phone-OTP only.
5. Enforceable refund SLA (is 7 working days the promise?).
6. Fast-track plan tier unlocking Stage-2 privilege toggles early for multi-block campuses.

---

## 8. Next recommended step

**Superseded 2026-07-29.** Phase 1 is long complete. The next work, in priority order:

1. **Re-grant `check_rate_limit` to `anon`** (or route OTP through a service-role path) so phone login works — coordinate with the in-flight MSG91 integration.
2. **Fix `fn_is_acting_as_student_only`** to require `auth.uid() IS NOT NULL`, then schedule `fn_scan_complaint_sla_breaches`. The trigger fix must land first or the scan will fail silently.
3. **Wire the dashboard KPIs to real queries** — the data is there; the cards ignore it.
4. **Give the admin complaint actions** (assign/resolve/comment) — the board is read-only today, which makes the whole complaints module non-operational for the superset role.
5. **Reconcile the two aging calculations** and remove the 500-row cap on `getAgingReport`.
6. Add a JS/TS test runner. There is still none; the QA harness lives outside the repo in a scratchpad and should be brought in-tree if these tests are to be kept.

Update Sec. 3, Sec. 5 and Sec. 6 of this file as each lands.

---

_End of Memory.md_
