# Local deploy / CLI validation only (copy to .env — gitignored).
# Generate: .\scripts\generate-sms-hook-secret.ps1
# Or copy existing secret from Dashboard → Authentication → Hooks → Send SMS.
#
# Must match Edge secret SEND_SMS_HOOK_SECRET on project umznrrdqduynifpatslb.
SEND_SMS_HOOK_SECRET=v1,whsec_REPLACE_WITH_GENERATED_OR_DASHBOARD_SECRET
